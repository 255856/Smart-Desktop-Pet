# 语音与模型资源下载说明

以下资源因体积或版权原因**不入 Git 仓库**，目录骨架已保留。下载后按说明放到对应位置即可。

## 1. 角色语音数据集（`voice/` 目录）

用于 GPT-SoVITS 训练/推理的参考与训练音频（wav + .lab 文本标注）。

| 角色 | 放置路径 | 下载链接 |
|---|---|---|
| 克拉拉 | `voice/克拉拉/` | （待补链接） |
| 安可 | `voice/安可/` | （待补链接） |
| 流萤 | `voice/流萤/` | （待补链接） |

要求：每段 wav **3~10 秒**（GPT-SoVITS 参考音频限制），配套同名 `.lab` 文本标注。

## 2. GPT-SoVITS 整合包（`GPT-SoVITS-v2pro-20250604-nvidia50/`）

v2ProPlus 版整合包（自带 Python runtime 与全部预训练模型，约 8GB）。

- 下载链接：（待补链接）
- 部署：解压到桌宠项目根目录下，保持目录名 `GPT-SoVITS-v2pro-20250604-nvidia50`
- 启动 TTS 服务：双击项目根目录的 **`start_tts_api.bat`**（或手动运行
  `runtime\python.exe api_v2.py -a 127.0.0.1 -p 9880 -c GPT_SoVITS\configs\tts_infer.yaml`）

## 3. 流萤微调模型（可选）

`v2ProPlus/流萤/流萤-e10.ckpt` 与 `流萤_e10_s250.pth`（别人训练好的流萤音色）。

- 下载链接：（待补链接）
- 放置：`GPT_weights_v2ProPlus/` 与 `SoVITS_weights_v2ProPlus/`（整合包内对应目录）
- 已在 `GPT_SoVITS\configs\tts_infer.yaml` 的 `custom` 段默认启用

## 常见问题

- **参考音频 3~10 秒限制**：合成报「参考音频在3~10秒范围外」时，换一段时长合规的 wav。
- **中文路径**：参考音频建议放在纯英文路径下（如本项目 `voice/` 内文件名含中文也可，
  服务端为 Windows 时通常可识别；若报「不存在」请改英文文件名）。
- 桌宠配置：`config.yaml` 的 `character.tts_engine: gptsovits` + 参考音频/文本，
  其余参数在桌宠设置面板「Live2D」页调整。
